# Durable Object migrations

Wrangler migration tags are declared in `wrangler.jsonc`. Add migration notes
here whenever room storage changes. Storage migrations must preserve cleanup and
expiration behavior for active rooms.
